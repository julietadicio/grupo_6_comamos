<!DOCTYPE html>
<html lang="en">

<head>
    <%- include partials/head %>
    <link rel="stylesheet" href="/css/account.css">
    <title>Editar Cuenta</title>
</head>

<body>
    <%- include partials/header %> 
    <main>
        <section class="box-space">
            <form action="/buisness/account/capacity/edit/<%= table.idTable %>/?_method=PUT" method="POST">
                <div class="flex-column">
                    <label for="nombre">Nombre de la Mesa</label>
                    <input type="text" name="nombre" id="nombre" value="<%= table.name %>">
                </div>
                <div class="flex-column">
                    <label for="ubicacion">Ubicación de la mesa</label>
                    <input type="text" name="ubicacion" id="ubicacion" value="<%= table.ubication %>">
                </div>
                <div class="flex-column">
                    <label for="estado">Estado</label>
                    <select name="estado" id="estado">
                        <option value="abierta" <%= table.status == 'abierta'? 'selected': null;%> >Abierta</option>
                        <option value="cerrada" <%= table.status == 'cerrada'? 'selected': null;%>>Cerrada</option>
                    </select>
                </div>
                <fieldset class="logout-button">
                    <button type="submit" class="none-decoration"><i class="fas fa-check"></i></button>
                </fieldset>
            </form>
            <form action="/buisness/account/capacity">
                <fieldset class="logout-button">
                        <button type="menu" class="none-decoration">Volver</button>
                </fieldset>    
            </form>
        </section>
    </main>
    <%- include partials/footer %>     
</body>

</html>